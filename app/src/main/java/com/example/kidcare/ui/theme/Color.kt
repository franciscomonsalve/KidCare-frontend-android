package com.example.kidcare.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// ── KidCare Accessibility Colors ─────────────────────────────────────────────
// WCAG AA: todos los colores de texto cumplen ratio ≥ 4.5:1 sobre fondo blanco

/** Texto principal sobre fondo blanco — ratio 16.1:1 */
val TextoPrincipal   = Color(0xFF111827)

/** Texto secundario (etiquetas, subtítulos) — ratio 9.7:1 */
val TextoSecundario  = Color(0xFF374151)

/** Texto terciario (metadatos, fechas) — ratio 5.9:1 */
val TextoTerciario   = Color(0xFF4B5563)

/** Placeholder de campo de texto — ratio 4.6:1 */
val TextoPlaceholder = Color(0xFF6B7280)

/** Texto de estado vacío / deshabilitado — ratio 4.5:1 */
val TextoVacio       = Color(0xFF6B7280)

/** Texto de error */
val TextoError       = Color(0xFFDC2626)

/** Color acento KidCare */
val AzulKidCare      = Color(0xFF2563EB)